# Cliente (se exponen al navegador: solo claves publicas)
VITE_SUPABASE_URL=https://<ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon key>
# Dominio del email sintetico: <usuario>@<dominio>. El usuario nunca lo ve.
VITE_AUTH_EMAIL_DOMAIN=hq.rockie.plus

# Solo servidor / scripts de QA (NUNCA con prefijo VITE_, nunca al repo)
# SUPABASE_SERVICE_ROLE_KEY=
# ANTHROPIC_API_KEY=        (fase 5: agente, se guarda como secret de la Edge Function)
# ANTHROPIC_MODEL=
